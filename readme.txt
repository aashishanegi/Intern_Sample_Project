pyhton3 -m venv venv : creates a venv folder meaning creates a virtual environment
preferred: because it removes any dependency conflicts
venv\Scripts\activate: virtual environment activated
deactivate:
pip install -r requirements.txt - jo bhi aap requirements.txt m daloge automatically install hojayega